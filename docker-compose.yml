services:
  bilidown:
    build: .
    image: bilidown:latest
    container_name: bilidown
    ports:
      - "8098:8098"
    volumes:
      # 数据持久化：数据库和下载文件
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
